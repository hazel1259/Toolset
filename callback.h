#ifndef __CALLBACK__
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Should return less than 0 if the item is less than the data item or greater than 0 if it after the item, 
// it should return 0 when the items are equal
//
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
typedef int (_stdcall * COMPAREPROC)(void * p1, void *p2);
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Object list stores data in the list, but does not know how to free it, calling this method deletes the 
// allocated data
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
typedef int (_stdcall * FREEDATAPROC) (void *p);
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// Displays the data in the object that calls, object and data must agree on type or it is broken
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
typedef void (__stdcall * DISPLAYDATAPROC) (void *p);

typedef int (__cdecl * COMPARESTRING)(const char * p1, const char * p2, size_t sz);
typedef int (__cdecl * COMPARESTRINGW)(const wchar_t * p1, const wchar_t *, size_t sz);

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// returns a string of data to be displayed
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#ifndef UNICODE 
#define PRINTPROC PRINTPROCA
#else
#define PRINTPROC PRINTPROCW
#endif
typedef int (__stdcall * PRINTPROCA)(char * pszData, int len);
typedef int (__stdcall * PRINTPROCW)(wchar_t  *pzwData, int len);
#endif